version: "1.0"
services:
  bot_service:
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    restart: always
    volumes:
      - ../app:/app
    environment:
      - HOST=0.0.0.0
      - PORT=5001
      - MODEL_PATH=Eka-Korn/distillbert-qa-tuned-lora_1.01_v2
      - BOT_TOKEN=7502004908:AAEMorcIXF31RgIce3OtGWa04CuaH1AWcFQ
    ports:
      - 5001:5001
